import Semantic.CoreCapybara.Compilation.CompilerCtx
open CoreCapybara
namespace Compilation

def CaptureSet.compile
  (ctx : CompilerCtx s1 s2)
  (cs : CaptureSet s1) :
  CaptureSet s2 := sorry

end Compilation
