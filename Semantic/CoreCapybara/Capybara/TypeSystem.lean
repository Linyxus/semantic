import Semantic.CoreCapybara.Capybara.TypeSystem.Core
-- NOTE: `BasicProps` is a stale verbatim copy of the core module's `BasicProps`
-- that was never adapted to Capybara's diverged type system; it does not yet
-- compile and is intentionally left out of the build path.
-- import Semantic.CoreCapybara.Capybara.TypeSystem.BasicProps
